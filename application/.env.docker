# ===========================================
# E-Commerce Analytics Platform
# Docker Environment Configuration
# ===========================================
# This file is specifically for Docker container environments
# It contains the exact configuration that works with your local MySQL setup

# ===========================================
# DATABASE CONFIGURATION (Host MySQL)
# ===========================================
# Using host.docker.internal to connect to host machine's MySQL server
# Your local MySQL is running on localhost:3306
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Root@123
DB_NAME=ecommerce_analytics

# ===========================================
# REDIS CONFIGURATION (Docker Redis)
# ===========================================
# Redis runs inside Docker network
REDIS_HOST=redis
REDIS_PORT=6379

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_ENV=development
DEBUG=true
PYTHONUNBUFFERED=1

# ===========================================
# STREAMLIT SETTINGS
# ===========================================
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_HEADLESS=true

# ===========================================
# NOTES
# ===========================================
# - host.docker.internal: Special DNS name that resolves to host machine IP
# - This allows Docker containers to access services on the host (your local MySQL)
# - Port 3306: Standard MySQL port (your local MySQL instance)
# - Password Root@123: Matches your working MySQL credentials
# - Redis uses service name 'redis' from docker-compose.yml