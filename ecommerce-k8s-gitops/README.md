# E-commerce Platform - GitOps Repository

GitOps repository for ArgoCD deployments.

## Quick Start

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/pratiksharajpure/ecommerce-k8s-gitops.git
git push -u origin main
```
